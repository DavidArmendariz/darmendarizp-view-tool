# frozen_string_literal: true

RSpec.describe DarmendarizpViewTool do
  it "has a version number" do
    expect(DarmendarizpViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
